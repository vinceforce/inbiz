class HeaderController < ApplicationController
  def index
    # @curr_user ||= User.find(session[:user_id]) if session[:user_id]
    # if @curr_user
    #     @current_contact = Contact.find_by(cont_contacts_mail_tx: @curr_user.email)
    # end
  end
end
